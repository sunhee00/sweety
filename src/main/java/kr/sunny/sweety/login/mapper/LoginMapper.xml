<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.sunny.sweety.login.mapper.LoginMapper"><!-- namespace:mapper.xml과 매핑될 mapper.java(인터페이스)를 의미-->
	
	<select id="loginCheck" resultType="kr.sunny.sweety.login.entity.LoginModel">
		select user_id, user_name
			from tb_user 
			where user_id = #{user_id} and user_pw = #{user_pw}
	</select>
	
	
</mapper>